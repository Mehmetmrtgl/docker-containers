version: '3.8'

services:
  converter:
    build: .
    container_name: rosbags_converter
    restart: "no"
    volumes:
      # Host yolunu aynen bağlıyoruz
      - /home/mehmet/urban39-pankyo/bag:/ros_ws/data
    tty: true
    stdin_open: true
